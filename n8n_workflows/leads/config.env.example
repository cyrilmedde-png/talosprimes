# Configuration pour le workflow "Traitement formulaire d'inscription"

# ============================================
# Configuration Resend (Email) - RECOMMANDÉ
# ============================================

# API Key Resend
RESEND_API_KEY=re_xxxxxxxxxxxxx

# Email d'envoi (doit être vérifié dans Resend)
EMAIL_FROM=TalosPrimes <noreply@talosprimes.com>

# Email de notification équipe
EMAIL_TO_EQUIPE=contact@talosprimes.com

# ============================================
# Configuration SMTP (Alternative - si vous n'utilisez pas Resend)
# ============================================

# Serveur SMTP
SMTP_HOST=smtp.gmail.com
# Port : 587 pour TLS, 465 pour SSL
SMTP_PORT=587
# Utilisez TLS (true) ou SSL (false)
SMTP_SECURE=true

# Identifiants SMTP
SMTP_USER=noreply@talosprimes.com
SMTP_PASSWORD=votre_mot_de_passe_app

# Email d'envoi (peut être différent de SMTP_USER)
# EMAIL_FROM=noreply@talosprimes.com
# EMAIL_FROM_NAME=TalosPrimes

# ============================================
# Configuration Twilio (SMS)
# ============================================

# Credentials Twilio
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=votre_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# Activer l'envoi de SMS (true/false)
SMS_ENABLED=true

# ============================================
# Configuration Slack (Optionnel)
# ============================================

# Webhook URL Slack pour notifications
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Canal Slack (optionnel, si pas dans l'URL)
SLACK_CHANNEL=#leads

# ============================================
# Configuration Discord (Optionnel)
# ============================================

# Webhook URL Discord pour notifications
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR/WEBHOOK/URL

# ============================================
# Configuration API TalosPrimes (Optionnel)
# ============================================

# URL de l'API backend pour sauvegarder les leads
API_URL=https://api.talosprimes.com
API_KEY=votre_api_key_ici

# ============================================
# Notes
# ============================================

# 1. RESEND (Recommandé pour les emails) :
#    - Obtenez votre API Key sur https://resend.com/
#    - Vérifiez votre domaine dans Resend
#    - Utilisez le nœud HTTP Request avec Header Auth
#    - Voir CONFIGURATION_RESEND_TWILIO.md pour plus de détails

# 2. TWILIO (Pour les SMS) :
#    - Obtenez vos credentials sur https://console.twilio.com/
#    - Créez une credential Twilio dans n8n (Settings → Credentials)
#    - Utilisez le nœud Twilio intégré dans n8n
#    - Voir CONFIGURATION_RESEND_TWILIO.md pour plus de détails

# 3. SMTP (Alternative - si vous n'utilisez pas Resend) :
#    - Pour Gmail : utilisez un "Mot de passe d'application"
#    - Pour SendGrid : SMTP_HOST=smtp.sendgrid.net, SMTP_USER=apikey
#    - Pour Mailgun : SMTP_HOST=smtp.mailgun.org

# 4. Ces variables doivent être configurées dans n8n :
#    - Settings → Variables (pour les variables d'environnement)
#    - Settings → Credentials (pour les API Keys sensibles)

