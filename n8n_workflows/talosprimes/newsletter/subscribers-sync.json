{
  "name": "Subscribers Sync",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 2 * * *"
            }
          ]
        }
      },
      "id": "01. Schedule Trigger",
      "name": "01. Schedule Trigger",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.1,
      "position": [100, 200]
    },
    {
      "parameters": {
        "jsCode": "const sql = `\nINSERT INTO subscribers (id, tenant_id, email, nom, prenom, source, status, created_at, updated_at)\nSELECT gen_random_uuid(), t.id, sub.email, sub.nom, sub.prenom, sub.source, 'active', NOW(), NOW()\nFROM (\n  SELECT DISTINCT ON (email) email, nom, prenom, 'lead'::text as source, tenant_id\n  FROM leads WHERE email IS NOT NULL AND email != ''\n  UNION ALL\n  SELECT DISTINCT ON (email) email, nom, prenom, 'client'::text as source, tenant_id\n  FROM client_finals WHERE email IS NOT NULL AND email != ''\n  UNION ALL\n  SELECT DISTINCT ON (email) email, nom, prenom, 'contact_form'::text as source, NULL as tenant_id\n  FROM contact_messages WHERE email IS NOT NULL AND email != ''\n) sub\nJOIN tenants t ON (sub.tenant_id = t.id OR sub.tenant_id IS NULL)\nON CONFLICT (tenant_id, email) DO NOTHING\nRETURNING id\n`;\n\nreturn { sql };"
      },
      "id": "02. Collect New Contacts",
      "name": "02. Collect New Contacts",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [300, 200]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "={{ $('02. Collect New Contacts').first().json.sql }}"
      },
      "id": "03. Postgres Execute Query",
      "name": "03. Postgres Execute Query",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2,
      "alwaysOutputData": true,
      "credentials": {
        "postgres": {"name": "Supabase Postgres", "id": "OGqj65ZoFRileCck"}
      },
      "position": [500, 200]
    },
    {
      "parameters": {
        "jsCode": "const result = $input.first().json;\nconst rowCount = (Array.isArray(result) ? result.length : 0);\n\nreturn {\n  status: 'success',\n  message: `Synced ${rowCount} new contacts to subscribers table`,\n  rowsInserted: rowCount,\n  timestamp: new Date().toISOString()\n};"
      },
      "id": "04. Log Result",
      "name": "04. Log Result",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [700, 200]
    }
  ],
  "connections": {
    "01. Schedule Trigger": {"main": [[{"node": "02. Collect New Contacts", "type": "main", "index": 0}]]},
    "02. Collect New Contacts": {"main": [[{"node": "03. Postgres Execute Query", "type": "main", "index": 0}]]},
    "03. Postgres Execute Query": {"main": [[{"node": "04. Log Result", "type": "main", "index": 0}]]}
  },
  "settings": {"executionOrder": "v1"},
  "active": true
}
