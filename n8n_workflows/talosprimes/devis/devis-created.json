{
  "name": "devis-created",
  "nodes": [
    {
      "parameters": {
        "path": "devis-created",
        "responseMode": "responseNode",
        "httpMethod": "POST"
      },
      "id": "01. Webhook",
      "name": "01. Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [100, 200]
    },
    {
      "parameters": {
        "jsCode": "const body = $input.first().json.body || $input.first().json;\n\nif (!body.tenantId) {\n  throw new Error('tenantId is required');\n}\nif (!body.clientFinalId) {\n  throw new Error('clientFinalId is required');\n}\n\nconst montantHt = parseFloat(body.montantHt) || 0;\nconst tvaTaux = parseFloat(body.tvaTaux) || 20;\nconst montantTtc = Math.round(montantHt * (1 + tvaTaux / 100) * 100) / 100;\n\nreturn [{ json: {\n  tenantId: body.tenantId,\n  clientFinalId: body.clientFinalId,\n  montantHt,\n  montantTtc,\n  tvaTaux,\n  dateDevis: body.dateDevis || new Date().toISOString(),\n  dateValidite: body.dateValidite || null,\n  description: body.description || null,\n  modePaiement: body.modePaiement || null,\n  lines: body.lines || []\n} }];"
      },
      "id": "02. Parser",
      "name": "02. Parser",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [300, 200]
    },
    {
      "parameters": {
        "conditions": {
          "string": [
            {
              "value1": "={{ $json.tenantId }}",
              "operation": "isNotEmpty"
            }
          ]
        }
      },
      "id": "03. Valide ?",
      "name": "03. Valide ?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 1,
      "position": [500, 200]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={ \"success\": false, \"error\": \"Validation failed\" }"
      },
      "id": "04. Respond Error",
      "name": "04. Respond Error",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [700, 100]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "=SELECT COALESCE(MAX(CAST(SUBSTRING(numero_devis FROM 'DEV-[0-9]{4}-([0-9]+)') AS INTEGER)), 0) + 1 as next_num FROM devis WHERE tenant_id = '{{ $json.tenantId }}' AND numero_devis LIKE 'DEV-' || EXTRACT(YEAR FROM NOW())::text || '-%'"
      },
      "id": "05. Get Next Number",
      "name": "05. Get Next Number",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2,
      "credentials": {
        "postgres": {
          "name": "Supabase Postgres",
          "id": "6Kosza77d9Ld32mw"
        }
      },
      "position": [700, 300]
    },
    {
      "parameters": {
        "jsCode": "const parser = $('02. Parser').first().json;\nconst nextNum = $('05. Get Next Number').first().json.next_num || 1;\nconst year = new Date().getFullYear();\nconst numeroDevis = 'DEV-' + year + '-' + String(nextNum).padStart(6, '0');\n\nreturn [{ json: { ...parser, numeroDevis } }];"
      },
      "id": "06. Generate Number",
      "name": "06. Generate Number",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [900, 300]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "=INSERT INTO devis (id, tenant_id, client_final_id, numero_devis, date_devis, date_validite, montant_ht, montant_ttc, tva_taux, description, mode_paiement, statut) VALUES (gen_random_uuid(), '{{ $('06. Generate Number').first().json.tenantId }}', '{{ $('06. Generate Number').first().json.clientFinalId }}', '{{ $('06. Generate Number').first().json.numeroDevis }}', '{{ $('06. Generate Number').first().json.dateDevis }}', {{ $('06. Generate Number').first().json.dateValidite ? \"'\" + $('06. Generate Number').first().json.dateValidite + \"'\" : 'NULL' }}, {{ $('06. Generate Number').first().json.montantHt }}, {{ $('06. Generate Number').first().json.montantTtc }}, {{ $('06. Generate Number').first().json.tvaTaux }}, {{ $('06. Generate Number').first().json.description ? \"'\" + $('06. Generate Number').first().json.description.replace(/'/g, \"''\") + \"'\" : 'NULL' }}, {{ $('06. Generate Number').first().json.modePaiement ? \"'\" + $('06. Generate Number').first().json.modePaiement + \"'\" : 'NULL' }}, 'brouillon') RETURNING id, tenant_id, client_final_id, numero_devis, date_devis, date_validite, montant_ht, montant_ttc, tva_taux, description, mode_paiement, statut, created_at, updated_at"
      },
      "id": "07. Insert Devis",
      "name": "07. Insert Devis",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2,
      "credentials": {
        "postgres": {
          "name": "Supabase Postgres",
          "id": "6Kosza77d9Ld32mw"
        }
      },
      "position": [1100, 300]
    },
    {
      "parameters": {
        "jsCode": "const devis = $('07. Insert Devis').first().json;\nconst parser = $('02. Parser').first().json;\nconst lines = parser.lines || [];\n\nif (lines.length === 0) {\n  return [{ json: { devisId: devis.id, lineInserts: [], hasLines: false } }];\n}\n\nconst lineInserts = lines.map((line, idx) => ({\n  devisId: devis.id,\n  codeArticle: line.codeArticle || null,\n  designation: line.designation || '',\n  quantite: line.quantite || 1,\n  prixUnitaireHt: line.prixUnitaireHt || line.prixUnitaireHT || 0,\n  totalHt: (line.quantite || 1) * (line.prixUnitaireHt || line.prixUnitaireHT || 0),\n  ordre: idx\n}));\n\nreturn [{ json: { devisId: devis.id, lineInserts, hasLines: true } }];"
      },
      "id": "08. Prepare Lines",
      "name": "08. Prepare Lines",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1300, 300]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "={{ $json.hasLines && $json.lineInserts.length > 0 ? $json.lineInserts.map(item => \"INSERT INTO devis_lines (id, devis_id, code_article, designation, quantite, prix_unitaire_ht, total_ht, ordre) VALUES (gen_random_uuid(), '\" + item.devisId + \"', \" + (item.codeArticle ? \"'\" + item.codeArticle + \"'\" : \"null\") + \", '\" + (item.designation || '').replace(/'/g, \"''\") + \"', \" + (item.quantite || 0) + \", \" + (item.prixUnitaireHt || 0) + \", \" + (item.totalHt || 0) + \", \" + (item.ordre || 0) + \")\").join(\"; \") + \"; SELECT 1\" : \"SELECT 1\" }}"
      },
      "id": "09. Insert Lines",
      "name": "09. Insert Lines",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2,
      "credentials": {
        "postgres": {
          "name": "Supabase Postgres",
          "id": "6Kosza77d9Ld32mw"
        }
      },
      "position": [1500, 300]
    },
    {
      "parameters": {
        "jsCode": "const devis = $('07. Insert Devis').first().json;\n\nreturn [{ json: {\n  id: devis.id,\n  tenant_id: devis.tenant_id,\n  client_final_id: devis.client_final_id,\n  numero_devis: devis.numero_devis,\n  date_devis: devis.date_devis,\n  date_validite: devis.date_validite,\n  montant_ht: devis.montant_ht,\n  montant_ttc: devis.montant_ttc,\n  tva_taux: devis.tva_taux,\n  description: devis.description,\n  mode_paiement: devis.mode_paiement,\n  statut: devis.statut,\n  created_at: devis.created_at,\n  updated_at: devis.updated_at\n} }];"
      },
      "id": "10. Format Response",
      "name": "10. Format Response",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1700, 300]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($json) }}"
      },
      "id": "11. Respond",
      "name": "11. Respond",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [1900, 300]
    }
  ],
  "connections": {
    "01. Webhook": {
      "main": [[{ "node": "02. Parser", "type": "main", "index": 0 }]]
    },
    "02. Parser": {
      "main": [[{ "node": "03. Valide ?", "type": "main", "index": 0 }]]
    },
    "03. Valide ?": {
      "main": [
        [{ "node": "05. Get Next Number", "type": "main", "index": 0 }],
        [{ "node": "04. Respond Error", "type": "main", "index": 0 }]
      ]
    },
    "05. Get Next Number": {
      "main": [[{ "node": "06. Generate Number", "type": "main", "index": 0 }]]
    },
    "06. Generate Number": {
      "main": [[{ "node": "07. Insert Devis", "type": "main", "index": 0 }]]
    },
    "07. Insert Devis": {
      "main": [[{ "node": "08. Prepare Lines", "type": "main", "index": 0 }]]
    },
    "08. Prepare Lines": {
      "main": [[{ "node": "09. Insert Lines", "type": "main", "index": 0 }]]
    },
    "09. Insert Lines": {
      "main": [[{ "node": "10. Format Response", "type": "main", "index": 0 }]]
    },
    "10. Format Response": {
      "main": [[{ "node": "11. Respond", "type": "main", "index": 0 }]]
    }
  }
}