{
  "name": "avoir-created",
  "nodes": [
    {
      "parameters": {
        "path": "avoir-created",
        "responseMode": "responseNode",
        "httpMethod": "POST"
      },
      "id": "01. Webhook",
      "name": "01. Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [
        100,
        200
      ]
    },
    {
      "parameters": {
        "jsCode": "var body = $input.first().json.body || $input.first().json;\n\nif (!body.tenantId) {\n  throw new Error('tenantId is required');\n}\n\nvar montantHt = parseFloat(body.montantHt) || 0;\nvar tvaTaux = parseFloat(body.tvaTaux) || 20;\nvar montantTtc = Math.round(montantHt * (1 + tvaTaux / 100) * 100) / 100;\n\nreturn [{ json: {\n  tenantId: body.tenantId,\n  clientFinalId: body.clientFinalId || null,\n  invoiceId: body.invoiceId || null,\n  montantHt,\n  montantTtc,\n  tvaTaux,\n  dateAvoir: body.dateAvoir || new Date().toISOString(),\n  motif: body.motif || null,\n  description: body.description || null,\n  lines: body.lines || []\n} }];"
      },
      "id": "02. Parser",
      "name": "02. Parser",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        300,
        200
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": ""
          },
          "conditions": [
            {
              "id": "341dfc7e-475c-4ebd-86bb-d6c71eb0ec54",
              "leftValue": "={{ $json.tenantId }}",
              "operator": {
                "type": "string",
                "operation": "notEmpty"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "03. Valide ?",
      "name": "03. Valide ?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        500,
        200
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={ \"success\": false, \"error\": \"Validation failed\" }"
      },
      "id": "04. Respond Error",
      "name": "04. Respond Error",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        700,
        100
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "={{ \"SELECT COALESCE(MAX(CAST(SUBSTRING(numero_avoir FROM 'AVO-[0-9]{4}-([0-9]+)') AS INTEGER)), 0) + 1 as next_num FROM avoirs WHERE tenant_id = '\" + $json.tenantId + \"' AND numero_avoir LIKE 'AVO-' || EXTRACT(YEAR FROM NOW())::text || '-%'\" }}"
      },
      "id": "05. Get Next Number",
      "name": "05. Get Next Number",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2,
      "credentials": {
        "postgres": {
          "name": "Supabase Postgres",
          "id": "OGqj65ZoFRileCck"
        }
      },
      "position": [
        700,
        300
      ],
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "var parser = $('02. Parser').first().json;\nvar nextNum = $('05. Get Next Number').first().json.next_num || 1;\nvar year = new Date().getFullYear();\nvar numeroAvoir = 'AVO-' + year + '-' + String(nextNum).padStart(6, '0');\n\nreturn [{ json: { ...parser, numeroAvoir } }];"
      },
      "id": "06. Generate Number",
      "name": "06. Generate Number",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        900,
        300
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "={{ \"INSERT INTO avoirs (id, tenant_id, client_final_id, invoice_id, numero_avoir, date_avoir, montant_ht, montant_ttc, tva_taux, motif, description, statut, created_at, updated_at) VALUES (gen_random_uuid(), '\" + $('06. Generate Number').first().json.tenantId + \"', \" + ($('06. Generate Number').first().json.clientFinalId ? \"'\" + $('06. Generate Number').first().json.clientFinalId + \"'\" : \"NULL\") + \", \" + ($('06. Generate Number').first().json.invoiceId ? \"'\" + $('06. Generate Number').first().json.invoiceId + \"'\" : \"NULL\") + \", '\" + $('06. Generate Number').first().json.numeroAvoir + \"', NOW(), \" + $('06. Generate Number').first().json.montantHt + \", \" + $('06. Generate Number').first().json.montantTtc + \", \" + ($('06. Generate Number').first().json.tvaTaux || 20) + \", \" + ($('06. Generate Number').first().json.motif ? \"'\" + $('06. Generate Number').first().json.motif + \"'\" : \"NULL\") + \", \" + ($('06. Generate Number').first().json.description ? \"'\" + $('06. Generate Number').first().json.description + \"'\" : \"NULL\") + \", 'brouillon', NOW(), NOW()) RETURNING id, tenant_id, client_final_id, invoice_id, numero_avoir, date_avoir, montant_ht, montant_ttc, tva_taux, motif, description, statut, created_at, updated_at\" }}"
      },
      "id": "07. Insert Avoir",
      "name": "07. Insert Avoir",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2,
      "credentials": {
        "postgres": {
          "name": "Supabase Postgres",
          "id": "OGqj65ZoFRileCck"
        }
      },
      "position": [
        1100,
        300
      ],
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "// Original prepare logic\nvar _origFn = function() {\nvar avoir = $('07. Insert Avoir').first().json;\nvar parser = $('02. Parser').first().json;\nvar lines = parser.lines || [];\n\nif (lines.length === 0) {\n  return [{ json: { avoirId: avoir.id, lineInserts: [], hasLines: false } }];\n}\n\nvar lineInserts = lines.map(function(line, idx) { return ({\n  avoirId: avoir.id,\n  codeArticle: line.codeArticle || null,\n  designation: line.designation || '',\n  quantite: line.quantite || 1,\n  prixUnitaireHt: line.prixUnitaireHt || line.prixUnitaireHT || 0,\n  totalHt: (line.quantite || 1) * (line.prixUnitaireHt || line.prixUnitaireHT || 0),\n  ordre: idx\n}); });\n\nreturn [{ json: { avoirId: avoir.id, lineInserts, hasLines: true } }];\n};\nvar _origResult = _origFn();\nvar prev = _origResult[0].json;\n\nvar items = prev.lineInserts || [];\n\nif (!prev.hasLines || items.length === 0) {\n  return [{ json: { ...prev, query: 'SELECT 1', params: [] } }];\n}\n\nvar params = [];\nvar idx = 1;\n\nvar values = items.map(function(item, i) { return {\n  var parentId = item.avoirId;\n  var codeArticle = item.codeArticle || null;\n  var designation = String(item.designation || '').substring(0, 500);\n  var quantite = parseFloat(item.quantite) || 0;\n  var prixUnitaireHt = parseFloat(item.prixUnitaireHt) || 0;\n  var totalHt = parseFloat(item.totalHt) || 0;\n  var ordre = parseInt(item.ordre) || i;\n\n  params.push(parentId, codeArticle, designation, quantite, prixUnitaireHt, totalHt, ordre);\n  var p = `(gen_random_uuid(), $$${idx}, $$${idx+1}, $$${idx+2}, $$${idx+3}, $$${idx+4}, $$${idx+5}, $$${idx+6})`;\n  idx += 7;\n  return p;\n}; }).join(',\n');\n\nvar query = `INSERT INTO avoir_lines (id, avoir_id, code_article, designation, quantite, prix_unitaire_ht, total_ht, ordre) VALUES ${values} RETURNING id, avoir_id, code_article, designation, quantite, prix_unitaire_ht, total_ht, ordre`;\n\nreturn [{ json: { ...prev, query, params } }];"
      },
      "id": "08. Prepare Lines",
      "name": "08. Prepare Lines",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1300,
        300
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "={{ $json.query }}"
      },
      "id": "09. Insert Lines",
      "name": "09. Insert Lines",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2,
      "credentials": {
        "postgres": {
          "name": "Supabase Postgres",
          "id": "OGqj65ZoFRileCck"
        }
      },
      "position": [
        1500,
        300
      ],
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "var avoir = $('07. Insert Avoir').first().json;\n\nreturn [{ json: {\n  id: avoir.id,\n  tenant_id: avoir.tenant_id,\n  client_final_id: avoir.client_final_id,\n  invoice_id: avoir.invoice_id,\n  numero_avoir: avoir.numero_avoir,\n  date_avoir: avoir.date_avoir,\n  montant_ht: avoir.montant_ht,\n  montant_ttc: avoir.montant_ttc,\n  tva_taux: avoir.tva_taux,\n  motif: avoir.motif,\n  description: avoir.description,\n  statut: avoir.statut,\n  created_at: avoir.created_at,\n  updated_at: avoir.updated_at\n} }];"
      },
      "id": "10. Format Response",
      "name": "10. Format Response",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1700,
        300
      ]
    },
    {
      "parameters": {
        "respondWith": "allIncomingItems"
      },
      "id": "11. Respond",
      "name": "11. Respond",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        1900,
        300
      ]
    }
  ],
  "connections": {
    "01. Webhook": {
      "main": [
        [
          {
            "node": "02. Parser",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "02. Parser": {
      "main": [
        [
          {
            "node": "03. Valide ?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "03. Valide ?": {
      "main": [
        [
          {
            "node": "05. Get Next Number",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "04. Respond Error",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "05. Get Next Number": {
      "main": [
        [
          {
            "node": "06. Generate Number",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "06. Generate Number": {
      "main": [
        [
          {
            "node": "07. Insert Avoir",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "07. Insert Avoir": {
      "main": [
        [
          {
            "node": "08. Prepare Lines",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "08. Prepare Lines": {
      "main": [
        [
          {
            "node": "09. Insert Lines",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "09. Insert Lines": {
      "main": [
        [
          {
            "node": "10. Format Response",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "10. Format Response": {
      "main": [
        [
          {
            "node": "11. Respond",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "active": true
}
