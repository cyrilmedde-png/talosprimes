{
  "name": "talosprimes",
  "version": "0.1.0",
  "private": true,
  "description": "SaaS de gestion d'entreprise orchestré par n8n",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter platform dev & pnpm --filter client dev",
    "build": "pnpm --filter shared build && pnpm --filter platform build && pnpm --filter client build",
    "build:shared": "pnpm --filter shared build",
    "build:platform": "pnpm --filter shared build && pnpm --filter platform build",
    "build:client": "pnpm --filter client build",
    "test": "pnpm --recursive test",
    "lint": "pnpm --recursive lint",
    "type-check": "pnpm --recursive type-check",
    "clean": "pnpm --recursive clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
